// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext.kotlin_version = '1.2.10'
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.0.1'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
    ext {
        projectBuildTools_version = '27.0.3'
        projectSdk_version = 27
        projectSdk_target_version = 27

        playServices_version = '11.8.0'
        supportLibs_version = '27.0.2'

        arch_lifecycle_version = '1.0.0'
        arch_room_version = '1.0.0'

        constraintLayout_version = '1.1.0-beta4'

        kotlinxCoroutines_version = '0.20'

        splitties_version = '1.3.0'

        timber_version = '4.6.0'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
